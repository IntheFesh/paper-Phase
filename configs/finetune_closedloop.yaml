policy: phaseqflow
stage: finetune_closedloop
notes: closed-loop verifier and replanning fine-tuning with correction data
phaseqflow:
  weak_phase_supervision_mode: hybrid
  replan_confidence_threshold: 0.5
  use_value_guided_weight: true
losses:
  verifier_bce: 1.0
  rollout_correction: 0.5
  recovery_segments: 0.5
